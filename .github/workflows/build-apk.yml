name: Build APK

on:
  push:
    branches:
      - 'claude/**'
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3
        with:
          cmdline-tools-version: latest
          accept-android-sdk-licenses: true
          log-accepted-android-sdk-licenses: false

      - name: 利用可能な SDK を確認
        run: |
          echo "=== installed ==="
          sdkmanager --list_installed || true
          echo "=== available platforms / build-tools ==="
          sdkmanager --list 2>/dev/null | grep -E "platforms;android-3[4-9]|build-tools;3[4-9]\." | sort -u || true

      - name: 必要な SDK を入れる
        run: |
          for p in "platforms;android-37" "platforms;android-36" "build-tools;36.0.0" "build-tools;35.0.0"; do
            yes | sdkmanager "$p" >/dev/null 2>&1 && echo "installed: $p" || echo "skipped: $p"
          done

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: core のユニットテスト
        run: ./gradlew :core:test

      - name: ktlint
        run: ./gradlew ktlintCheck

      - name: スマホ APK
        run: ./gradlew :app:assembleDebug

      - name: Wear OS APK
        run: ./gradlew :wear:assembleDebug

      - name: APK をまとめる
        if: success()
        run: |
          mkdir -p artifacts
          cp app/build/outputs/apk/debug/app-debug.apk artifacts/timtra-phone-debug.apk
          cp wear/build/outputs/apk/debug/wear-debug.apk artifacts/timtra-wear-debug.apk
          ls -la artifacts

      - name: APK をアップロード
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: timtra-apk
          path: artifacts/*.apk
          if-no-files-found: error
          retention-days: 30

      # スマホから直接インストールできるよう、開発版リリースにも APK を置く
      - name: 開発版リリースを更新
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete dev --yes --cleanup-tag 2>/dev/null || true
          gh release create dev artifacts/*.apk \
            --target "$GITHUB_SHA" \
            --title "TimTra 開発版" \
            --prerelease \
            --notes "コミット ${GITHUB_SHA} の debug ビルド。

          - timtra-phone-debug.apk — スマートフォン用
          - timtra-wear-debug.apk — Wear OS 用（スマホ版と同じ debug 署名。Data Layer 同期のため両方入れる）

          時刻表はまだサンプルデータです。実ダイヤではありません。"

      - name: テストレポートをアップロード（失敗時）
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: reports
          path: |
            */build/reports/**
          retention-days: 7
