# TimTra — Android + Wear OS 通勤ダイヤアプリ

あなたはこのプロジェクトの実装担当です。以下の仕様に従って開発してください。
このテキストの内容は CLAUDE.md としてリポジトリ直下に保存し、以降の作業の基準にしてください。

## 0. プロジェクト基本情報

- アプリ名: TimTra（Timetable + Transit）
- パッケージ名: com.kazuya.timtra
- 対象: Android スマートフォン + Wear OS
- 用途: 個人の通勤専用。汎用の経路検索アプリではない。
- 言語: Kotlin / UI表示は日本語

## 1. 何を解決するアプリか

日ノ丸バスとJR山陰本線を乗り継ぐ通勤で、
「家を何時に出ればいいか」「今日のバスでJRに間に合うか」を即座に答える。

JR山陰本線の鳥取〜宝木間は本数が少なく、鳥取駅で1本逃すと大きな待ちが発生する。
これを防ぐことが本アプリの最大の存在理由。

## 2. 対象経路（固定）

往路（朝・出勤）
  自宅 →徒歩→ 南吉成 バス停
       →日ノ丸バス 用瀬智頭線(91-95) 鳥取駅方面→ 鳥取駅バスターミナル
       →徒歩（乗換）→ JR鳥取駅
       →JR山陰本線 下り（浜村・倉吉方面）→ JR宝木駅
       →徒歩/自転車→ 勤務先

復路（夕・帰宅）
  勤務先 → JR宝木駅
       →JR山陰本線 上り（鳥取方面）→ JR鳥取駅
       →徒歩（乗換）→ 鳥取駅バスターミナル
       →日ノ丸バス 用瀬智頭線 用瀬・智頭方面→ 南吉成 →徒歩→ 自宅

往路・復路は時刻帯で自動判定し、手動切替も可能にする。

## 3. 設計の原則（重要・必ず守る）

1. JRをアンカーにする。
   乗りたいJR便を先に決め、そこから逆算してバス便と出発時刻を求める。
   バス便を先に決めてJRを探す実装にはしないこと。

2. バス便は「間に合う中で最も遅い便」を選ぶ。
   早い便を選ぶと無意味に早起きすることになる。

3. core モジュールは Android SDK に依存させない。
   乗り継ぎ計算は純粋な Kotlin で書き、JVMユニットテストで検証できる状態を保つ。
   スマホと Wear で計算結果が必ず一致すること。

4. バックグラウンド常駐禁止。
   通知は前夜に WorkManager で計算し、AlarmManager で予約する。
   常時ポーリングや位置情報の常時取得はしない。

5. すべて端末内で完結させる。サーバーは構築しない。

## 4. データソース

### 4-1. バス時刻表（GTFS-JP）

- 出典: 鳥取県オープンデータ（バス情報） https://odp-pref-tottori.tori-info.co.jp/bus.html
- 事業者: 日ノ丸自動車（GTFS作成委託先: ジョルダン株式会社）
- 対象路線: 用瀬智頭線（系統 91-95）
- 対象停留所: 南吉成、鳥取駅

取り扱い方針:
- GTFS-JP の ZIP を取得し、tools/gtfs_import.py でビルド時に
  対象路線・対象停留所のみを抽出し、Room のプリパッケージDBを生成する。
- 実行時に GTFS ZIP をダウンロードしないこと。起動速度とオフライン動作を優先する。
- ダイヤ改正時は変換スクリプトを再実行してアプリを更新する運用とする。

最初にやる作業:
- stops.txt から stop_name に「南吉成」「鳥取駅」を含む行を検索し stop_id を特定する。
  鳥取駅は乗り場ごとに複数の stop_id に分かれている可能性が高いので、
  routes.txt / trips.txt を用瀬智頭線で絞ってから stop_times.txt と突き合わせて確定させる。
- 用瀬智頭線の route_id と、方向ごとの direction_id を確定させる。
- 判明した ID は docs/ids.md に記録し、コード内にハードコードせず定数にまとめる。

### 4-2. バス リアルタイム（GTFS-RT）

- 鳥取県が 2024年10月から提供。VehiclePosition（車両位置）が対象。
- 取得は30秒に1回まで。これはデータ提供元の明示条件であり、
  必ずレート制限をクライアント側に実装して破らないこと。
- ポーリングは画面がフォアグラウンドにある間のみ行う。
- 提供が VehiclePosition のみで TripUpdate（遅延予測）が含まれない場合は、
  当該 trip の車両位置と stop_times 上の予定位置を比較して遅延を推定する。
  例:「予定ではすでに南吉成を出ているはずだが、車両はまだ2停留所手前」→ 遅延と判断。
  推定値であることを UI に必ず明示する（例:「約5分遅れ（推定）」）。
- ライブラリは com.google.transit:gtfs-realtime-bindings を使用。

### 4-3. JR時刻表（手動データ）

- JR西日本は時刻表のオープンデータを提供していない。
- JR西日本のサイトをスクレイピングするコードは書かないこと。利用規約上の懸念がある。
- 対象2駅・対象方向の時刻のみを JSON でアプリに同梱する。
  ダイヤ改正は原則年1回なので、年1回の手作業更新で運用可能。
- 配置: app/src/main/assets/jr_timetable.json
- スキーマ:

  {
    "version": "2026-03-14",
    "note": "2026年3月ダイヤ改正版。出典: JR西日本 駅時刻表（手動転記）",
    "legs": [
      {
        "id": "tottori_to_hougi",
        "from": "鳥取",
        "to": "宝木",
        "line": "山陰本線",
        "services": [
          {
            "trainId": "1234D",
            "calendar": "weekday",
            "departure": "07:12",
            "arrival": "07:34",
            "platform": "0",
            "note": ""
          }
        ]
      },
      {
        "id": "hougi_to_tottori",
        "from": "宝木",
        "to": "鳥取",
        "line": "山陰本線",
        "services": []
      }
    ],
    "overrides": [
      { "date": "2026-12-31", "calendar": "holiday" }
    ]
  }

- calendar は weekday / saturday / holiday / everyday を取る。
- 実データはまだ入力していない。空配列でスキーマだけ用意し、
  サンプル数便を入れて動作確認できる状態にすること。

## 5. モジュール構成

  timtra/
  ├── core/            Kotlin純粋ロジック（Android非依存）
  │   ├── model/       Stop, Trip, ServiceCalendar, Leg, Journey
  │   ├── data/        Room DAO, GTFS Repository, JR JSON Repository
  │   ├── realtime/    GTFS-RT クライアント（30秒スロットリング内蔵）
  │   └── journey/     乗り継ぎ計算エンジン（本アプリの心臓部）
  ├── app/             スマホ（Jetpack Compose）
  │   ├── ui/          ホーム、時刻表一覧、設定
  │   ├── notify/      WorkManager + AlarmManager
  │   └── widget/      ホーム画面ウィジェット（Glance）
  ├── wear/            Wear OS
  │   ├── tile/        タイル（次の便）
  │   ├── complication/
  │   └── ui/
  └── tools/
      └── gtfs_import.py

## 6. 乗り継ぎ計算エンジン（core/journey）

### ユーザー設定値と既定値

  自宅→南吉成 徒歩            5分   （家を出る時刻の逆算に使用）
  鳥取駅 バス降車→JRホーム    8分   （乗換に必要な時間）
  準備時間（バッファ）         5分   （通知を早める余裕）
  宝木駅→勤務先               10分  （到着予測表示用）
  最低乗換許容                5分   （これを下回る接続は危険判定）

すべて設定画面から変更可能にすること。既定値は仮であり、後で調整する。

### 出力データ構造

  Journey {
    targetTrain: JRService      乗りたいJR便
    busTrip: BusTrip            それに間に合うバス便（最も遅い便）
    leaveHomeAt: LocalTime      家を出る時刻
    transferMargin: Duration    鳥取駅での余裕
    status: OK | TIGHT | RISK | MISSED
    fallback: Journey?          危険時の代替案（1本前のバス等）
  }

### 計算手順

1. 現在時刻以降のJR便を列挙し、直近の1本を targetTrain とする。
2. targetTrain.departure から「乗換時間 + 最低乗換許容」を引いた時刻までに
   鳥取駅へ到着するバス便のうち、最も遅い便を選ぶ。
3. リアルタイム情報があれば推定遅延を加算して再判定する。
4. transferMargin により状態を分類する。
     10分以上   → OK（緑）
     5〜10分    → TIGHT（オレンジ）
     5分未満    → RISK（赤 + 「1本前のバスに変更」を提示）
     乗車済みで間に合わない → MISSED（次のJR便に切り替えて再計算）
5. leaveHomeAt = バス発車時刻 − 自宅→バス停徒歩 − 準備時間

## 7. 画面

### 7-1. スマホ ホーム（最重要・一画面完結）

上から順に配置する。

1. 家を出る時刻（最大フォント）と、そこまでの残り時間
2. バス: 南吉成 発 07:05 → 鳥取駅 着 07:24（遅延があれば併記）
3. 乗り継ぎ: 鳥取駅 余裕 9分 + ステータスバッジ（色分け）
4. JR: 鳥取 発 07:33 → 宝木 着 07:55
5. 到着予測: 勤務先 08:05
6. 次の候補（1本後）を折りたたみで表示

### 7-2. 時刻表一覧

南吉成 / 鳥取駅 / 宝木 の各時刻表を素の一覧で表示。現在時刻の位置に自動スクロール。

### 7-3. 設定

徒歩時間などの調整、通知ON/OFF、通知タイミング、往路/復路の時刻帯設定、
「今日は休み」トグル（有給・出張時に通知を止める）。

### 7-4. Wear OS

- タイル: 家を出る時刻 + 残り時間 + ステータス色。タップでバス/JRの発時刻詳細。
- コンプリケーション: 残り時間のみ（SHORT_TEXT）。文字盤に常時表示。
- Wearable Data Layer API でスマホから計算済み Journey を同期。
  同梱データがあるので Watch 単独でも計算可能にしておく。

## 8. 通知設計

方針: 常駐サービスやバックグラウンドポーリングは使わない。
前夜に翌日分の通知をローカル予約することで、電池も権限も最小で済ませる。

- WorkManager の日次ジョブ（毎晩23:00頃）で翌日分の Journey を計算する。
- 各通知を AlarmManager.setExactAndAllowWhileIdle() で予約する。

往路で予約する通知:

  家を出る時刻の10分前   「あと10分で出発。7:05のバスです」
  家を出る時刻           「出発時刻です」
  バス発車の3分前        「まもなく南吉成発。乗り遅れ注意」
  鳥取駅到着予定の2分前  「次は鳥取駅。JRは07:33発」

復路も終業時刻を基準に同様に予約する。

注意点:
- 初回起動時に SCHEDULE_EXACT_ALARM 権限と、
  バッテリー最適化の除外を案内する導線を必ず実装すること。
  これを省くと通知が遅れる、または飛ぶ。
- 祝日・年末年始は calendar_dates.txt と JR JSON の overrides を見て通知を抑止する。

## 9. 技術スタック

  言語          Kotlin
  UI            Jetpack Compose / Compose for Wear OS
  DB            Room（プリパッケージDB）
  非同期        Coroutines + Flow
  DI            Hilt
  通信          Ktor Client または Retrofit + OkHttp
  GTFS-RT       com.google.transit:gtfs-realtime-bindings
  定期処理      WorkManager
  正確な通知    AlarmManager (setExactAndAllowWhileIdle)
  ウィジェット  Glance
  端末間同期    Wearable Data Layer API
  minSdk        26（Wear OS 3以降を想定するなら30）

## 10. コーディング規約

- Kotlin 公式スタイル。ktlint 準拠。
- 時刻は java.time（LocalTime / LocalDate / Duration）を使う。Date / Calendar は使わない。
- タイムゾーンは Asia/Tokyo 固定でよいが、ハードコードせず定数にまとめる。
- UIは Jetpack Compose。XMLレイアウトは新規に作らない。
- 表示文言は日本語。strings.xml に集約する。

## 11. テストで必ず押さえる境界条件

- 深夜0時をまたぐ便（GTFSの 24:15:00 形式の時刻表記に対応すること）
- 最終便より後の時刻に起動したとき（翌日の初便を表示する）
- 土曜 / 日曜祝日ダイヤの切り替え
- calendar_dates.txt による運休日・特別運行日
- 年末年始
- 乗り継ぎ余裕がちょうど境界値のとき（OK / TIGHT / RISK の判定）

## 12. 開発順序

1. tools/gtfs_import.py を書き、GTFS-JPから対象路線・停留所を抽出してDB化する
2. core/journey の乗り継ぎ計算 + ユニットテスト
3. スマホ ホーム画面（静的データのみで動く状態にする）
4. 通知スケジューラ
5. Wear OS タイル + コンプリケーション
6. GTFS-RT 連携（遅延推定・30秒スロットリング）
7. ホーム画面ウィジェット

3 の時点で実用になる。4 まで来れば当初の目的をほぼ満たす。
一気に全部作らず、この順で段階的に動く状態を保ちながら進めること。

## 13. やらないこと

- 経路検索の一般化（他の駅・他の路線への対応）
- サーバーサイドの構築
- ユーザーアカウント、クラウド同期
- 広告、課金

## 14. 出典表示

アプリ内「このアプリについて」画面に以下を必ず記載すること。

- バス時刻・車両位置情報: 鳥取県オープンデータ（日ノ丸自動車）
- JR時刻: JR西日本 駅時刻表より手動転記。最新情報は公式サイトでご確認ください。

## 15. 最初のタスク

まず 12 の手順 1 から着手してください。
着手前に、不明点や仕様の矛盾があれば質問してください。
